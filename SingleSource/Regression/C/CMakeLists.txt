if(ARCH STREQUAL "x86")
  if(DEFINED USE_REFERENCE_OUTPUT)
    set(EXEC_XFAILS casts)
  endif()
endif()
list(APPEND LDFLAGS -lm)
list(APPEND CFLAGS -mno-sse -mno-sse2 -mno-avx)
llvm_singlesource(PREFIX "regression-c")
